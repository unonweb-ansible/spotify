# NOTES

A **legacy way** to setup the repo is also available (using .list format) because the spotify key-repo relation (formerly) seemed not to be set up correctly. 

- We put the key under /etc/apt/trusted.gpg.d/ instead of /etc/apt/keyrings
- We use spotify.list instead of spotify.sources
